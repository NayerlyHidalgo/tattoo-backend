services:
  - type: web
    name: tattoo-backend-api
    env: node
    region: oregon
    plan: free

    rootDir: .
    buildCommand: node build-script.js
    startCommand: cd dist && node main
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
